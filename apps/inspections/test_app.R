# Test script for inspections app with prehatch_only toggle
# Run this to test the new prehatch functionality

# Set working directory to the app folder
setwd("c:/Users/datatech/Documents/mmcd_metrics/apps/inspections")

# Run the app
shiny::runApp()
